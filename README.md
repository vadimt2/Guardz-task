# Guardz Task

## Backend

Run the backend using Docker Compose:

```bash
cd backend
docker compose up --build
```

## Frontend

Run the frontend development server:

```bash
cd frontend
npm install
npm run dev
```
